
#include <string>
#include <vector>
#include <stack>

using namespace std;

int solution(vector<vector<int>> board, vector<int> moves) {
    //인형을 저장한 stack
    stack<int> dollStore;
    //크레인이 내려간 위치에 인형이 있는지 검사한다.
    for(int i = 0; i < moves.size(); i++){
        int crainPoint = moves[i];
        
        for(int j = 0; j < )
        
    }
    int answer = 0;
    return answer;
}
